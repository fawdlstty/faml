[package]
name = "famllib"
version = "0.1.3"
edition = "2024"

[lib]
name = "famllib"
crate-type = ["cdylib", "staticlib"]

[dependencies]
faml = { path = "../faml" }

[build-dependencies]
cbindgen = "0.29.0"
